services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weather_report_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
